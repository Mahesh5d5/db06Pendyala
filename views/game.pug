extends layout

block content
  h1= title
  //- - let items = ['designer','price','size']
  - let items = results;
  - let o;
  script.
    function buy(x){
      alert('purchased item ' + x)
   } 
  div(class='container-fluid')
    div(class='row m-1')
    div(class="row row-cols-1 row-cols-sm-4 g-6")
      each o, index in items
       - let destination = `dispatch(\"/costumes/update?id=${item._id}\")` 
       div(class="row-sm-2", class="m-1") Item #{item. designer}(#{item.price}) is #{item.size}
       button.btn( class="btn-primary ml-1", onclick= destination) Update
        div(class="card text-dark bg-warning mb-3" class='col' class='card' class='card-body' class='card-text' onclick='buy('+index+')') #{o}
      
      
      script.
     
      function buy(itemNo)
      {
        alert(`You purchased Item ${itemNo+1}`)
      } 
      function dispatch(path){ 
      window.location.href=path 
      }  
        

  
